server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: any unmatched path falls back to index.html so
    # client-side routing (if added later) still works.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Static assets emitted by the Vite build are content-hashed, so they
    # can be cached aggressively.
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

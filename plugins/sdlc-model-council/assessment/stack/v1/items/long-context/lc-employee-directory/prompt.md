# Long-context lookup: employee directory

Below is the full contents of `employee_directory.txt`, an internal employee
directory listing. Somewhere in this directory, exactly one record includes a
line stating a "Special access code" for that employee.

Find the record for employee ID `EMP-0761` and report the special access code
value given for that employee (the text inside the single quotes after
"the code is").

{{INPUT:employee_directory.txt}}

Respond with ONLY the answer text on a single line; no explanation.

"""sdlc-workflows plugin scripts.

Packaged so tests (and other plugin code) can import via
``from sdlc_workflows_scripts import resolve_credentials``.

The individual modules remain directly executable as CLIs
(``python3 scripts/foo.py``), as invoked by skills and bash nodes.
"""
